{% extends 'base.html.twig' %}

{% block body %}
    <h1>{% if form.vars.value.id %}Modifier{% else %}Ajouter{% endif %} un produit</h1>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Enregistrer</button>
    {{ form_end(form) }}
    <a href="{{ path('admin_dashboard') }}">Retour</a>
{% endblock %}
